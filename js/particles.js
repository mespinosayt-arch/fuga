window.FugaFlow=(()=>{let canvas,ctx,items=[],raf;function init(id){canvas=document.getElementById(id);ctx=canvas.getContext("2d");window.addEventListener("resize",resize);resize();loop()}function resize(){if(!canvas)return;const r=canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);canvas.width=r.width*d;canvas.height=r.height*d;ctx.setTransform(d,0,0,d,0,0)}function setLeaks(leaks,monthly){const width=canvas?.getBoundingClientRect().width||300;items=[];leaks.forEach(l=>{const ratio=Math.max(.08,l.monthly/monthly);const count=Math.ceil(ratio*20);for(let i=0;i<count;i++)items.push({x:Math.random()*width,y:30+Math.random()*125,v:.3+ratio*1.8,r:1+ratio*5+Math.random()*2,color:l.color,phase:Math.random()*6.28})})}function loop(t=0){if(!ctx||!canvas)return;const w=canvas.clientWidth,h=canvas.clientHeight;ctx.clearRect(0,0,w,h);const grad=ctx.createLinearGradient(0,0,w,0);grad.addColorStop(0,"rgba(255,255,255,0)");grad.addColorStop(.45,"rgba(201,255,63,.16)");grad.addColorStop(1,"rgba(255,255,255,0)");ctx.strokeStyle=grad;ctx.lineWidth=1;for(let j=0;j<3;j++){ctx.beginPath();for(let x=0;x<w;x+=5){const y=h/2+Math.sin(x/45+t/1100+j)*15+j*9-9;x?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.stroke()}items.forEach(p=>{p.x+=p.v;if(p.x>w+15)p.x=-15;p.y+=Math.sin(t/900+p.phase)*.08;ctx.globalAlpha=.75;ctx.fillStyle=p.color;ctx.shadowBlur=14;ctx.shadowColor=p.color;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()});ctx.shadowBlur=0;ctx.globalAlpha=1;raf=requestAnimationFrame(loop)}return{init,setLeaks}})();
